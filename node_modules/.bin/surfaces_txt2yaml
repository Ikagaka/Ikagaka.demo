#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../surfaces_txt2yaml/bin/surfaces_txt2yaml.js" "$@"
  ret=$?
else 
  node  "$basedir/../surfaces_txt2yaml/bin/surfaces_txt2yaml.js" "$@"
  ret=$?
fi
exit $ret
